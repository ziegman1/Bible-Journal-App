/** Target people to share with per pillar week—Sun–Sat in practice timezone (BADWR dashboard rhythm). */
export const DEFAULT_SHARE_WEEKLY_GOAL_ENCOUNTERS = 5;

/** @deprecated Use profile `weekly_share_goal_encounters` via {@link fetchUserRhythmGoals}; this is the app default only. */
export const SHARE_WEEKLY_GOAL_ENCOUNTERS = DEFAULT_SHARE_WEEKLY_GOAL_ENCOUNTERS;
